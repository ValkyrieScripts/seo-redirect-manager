# API URL (optional - defaults to /api which proxies to backend)
# For development with separate backend:
# VITE_API_URL=http://localhost:3001/api

# For production, leave empty to use the proxy
VITE_API_URL=
